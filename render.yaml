services:
  - type: web
    name: intraday-ai
    runtime: python
    plan: starter
    buildCommand: "python -m py_compile app.py"
    startCommand: "python app.py"
    healthCheckPath: "/health"
    disk:
      name: intraday-data
      mountPath: /var/data
      sizeGB: 1
